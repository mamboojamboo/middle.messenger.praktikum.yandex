<form class="{{ styles.user-info }}">
    <div>
        {{{ email }}}
        {{{ login }}}
        {{{ firstName }}}
        {{{ lastName }}}
        {{{ nickName }}}
        {{{ phone }}}
        {{{ avatar }}}
    </div>

    {{#if isEdit}}
        <div>
            {{{ submitButton }}}
        </div>
    {{else}}
        <div class="{{ styles.user-info__buttons-container}}">
            <button
                id="change-data"
                class="{{ styles.user-info__button }}"
                type="button"
                data-main="CHAT_CHANGE_DATA"
            >
                Change personal data
            </button>
            <button
                id="change-password"
                class="{{ styles.user-info__button }}"
                type="button"
                data-main="CHAT_CHANGE_PASSWORD"
            >
                Change password
            </button>
        </div>
    {{/if}}
</form>
