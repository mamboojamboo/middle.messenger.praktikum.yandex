<label class="{{ styles.input__container }}">
	{{ label }}
	<input
		class="{{ styles.input__field }}"
		type="{{ type }}"
		name="{{ name }}"
		placeholder="{{ placeholder }}"
		required="{{ required }}"
	>
</label>
