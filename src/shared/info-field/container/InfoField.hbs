<div class="{{ styles.container }}">
    <div>{{{ title }}}</div>

    {{#if isEdit}}
        <input name="{{ name }}" value="{{ value }}" />
    {{else}}
        <div>{{{ value }}}</div>
    {{/if}}
</div>
