{
    "extends": "stylelint-config-standard",
    "rules": {
        "custom-property-pattern": "[a-z-0-9,\\s]+",
        "selector-class-pattern": "^.([a-z](-[a-z0-9]+)*)+(__[a-z0-9]+(-[a-z0-9]+)?)?(--[a-z0-9]+(-[a-z0-9]+)?){0,2}$"
    },
    "ignoreFiles": [
        "dist/**/*.css"
    ]
}
